{
  "name": "offsite-employee-login",
  "version": "1.0.0",
  "private": true,
  "description": "Attendance & Location Tracker — pure client-side (HTML/CSS/JS), hosted on GitHub Pages",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "check": "node --check public/data.js && node --check public/app.js && node --check public/config.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
